return {
  config = {
    title = '',
    lib = 'switch',
    pin = nil,
    delay = 0, -- Плавность включения
    onchange = false -- Броадкаст статуса при смене значения
  },
  init= function(config)
  end,
  get = function()
  end,
  set = function (value)
  end,
  status = function()
  end
}
